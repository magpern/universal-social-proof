# OS / editor
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# PHP / Composer (future M0+)
/vendor/
composer.lock

# Node (future acceptance only)
/node_modules/
npm-debug.log*

# Test / coverage artifacts (future)
/tests/tmp/
/.phpunit.result.cache
/coverage/

# Local env / secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
